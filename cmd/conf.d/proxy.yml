# 网络配置
server:
  # 服务器模式
  mode: "proxy"
  # 允许局域网其它地址连接
  allow_lan: true
  # 绑定地址，默认： 0.0.0.0
  bind: "0.0.0.0"
  # Http代理端口
  http_port: 1080
  # Socks代理端口
  socks_port: 1081

# Http 代理配置
http:
  disabled: true
  cert-file: ""

# Socks5 代理配置
socks:
  disabled: false
  allow_no_auth: true